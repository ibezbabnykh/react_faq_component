import React from 'react';

import './app-header.scss';

const AppHeader = () => {
    return (
        <div className="app-header">
            <h1>FAQ</h1>
        </div>
    );
};

export default AppHeader;